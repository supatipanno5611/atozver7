# 특정 마크다운 파일 열기

## 역할

설정의 `CertainMdPath`에 지정한 마크다운 파일을 빠르게 엽니다.

## 사용 방법

- 명령 팔레트에서 `특정 마크다운 파일 열기`를 실행합니다.
- 이미 같은 파일이 메인 작업 영역에 열려 있으면 해당 탭으로 이동합니다.
- 열려 있지 않으면 새 탭에서 파일을 엽니다.

## 설정 및 제약

- 설정에서 대상 파일 경로가 지정되어 있어야 합니다.
- 경로가 비어 있거나 파일이 없으면 알림만 표시하고 문서를 열지 않습니다.
